#include <iostream>
#include <fstream>
int main(){
    std::ifstream in("input.txt");
    if(in==NULL) return -1;
    std::ofstream out(output.txt);
    if(out==NULL) return -2;
    int n;
    in>>n;
    while(in!=eof())

}